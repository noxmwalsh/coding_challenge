<div class="container mx-auto px-4 py-8">
  <div class="bg-white rounded-lg shadow-md p-6">
    <div class="flex justify-between items-start mb-6">
      <div>
        <h1 class="text-3xl font-bold mb-2"><%= @post.title %></h1>
        <p class="text-gray-600 mb-4"><%= @post.description %></p>
        <div class="flex items-center text-sm text-gray-500">
          <p>By <%= @post.author %></p>
          <span class="mx-2">•</span>
          <p><%= time_ago_in_words(@post.created_at) %> ago</p>
        </div>
      </div>
      <% if user_signed_in? && @post.user == current_user %>
        <div class="flex space-x-2">
          <%= link_to 'Edit', edit_post_path(@post), class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded' %>
          <%= button_to 'Delete', @post, method: :delete, data: { confirm: 'Are you sure?' }, class: 'bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded' %>
        </div>
      <% end %>
    </div>

    <div class="prose max-w-none">
      <%= simple_format(@post.body) %>
    </div>
  </div>

  <div class="mt-8">
    <%= link_to 'Back to Posts', posts_path, class: 'text-blue-600 hover:text-blue-800' %>
  </div>
</div> 